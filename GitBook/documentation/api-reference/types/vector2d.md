# ⚙️ Vector2D

```
Constructor : Vector2D()
Constructor : Vector2D(int16_t, int16_t)
Member Variables : x, y, Lerp(Vector2D, DeltaTime), Min(Vector2D),
    Max(Vector2D), DistanceTo(Vector2D), All arrithmic operations
```
