# ⚙️ Vector4D

```
Constructor : Vector4D()
Constructor : Vector4D(float, float, float, float)

Member Variables : w, x, y, z, Lerp(Vector4D, DeltaTime), Min(Vector4D),
    Max(Vector4D), DistanceTo(Vector4D), All arrithmic operations
```
