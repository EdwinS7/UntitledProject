# ⚙️ Rect

```
Constructor : Rect()
Constructor : Rect(int16_t, int16_t, int16_t, int16_t)

Member Variables : x, y, w, h, Lerp(Rect, DeltaTime), Min(Rect),
    Max(Rect), DistanceTo(Rect), All arrithmic operations
```
